# Note: In production, use external secret management (e.g., Vault, AWS Secrets Manager)
# This is a template - DO NOT commit real secrets to git
apiVersion: v1
kind: Secret
metadata:
  name: exchange-secrets
type: Opaque
stringData:
  database.username: "exchange_user"
  database.password: "CHANGE_ME_IN_PRODUCTION"
  redis.password: "CHANGE_ME_IN_PRODUCTION"
  jwt.secret: "CHANGE_ME_TO_RANDOM_32_CHARS_OR_MORE_IN_PRODUCTION"
